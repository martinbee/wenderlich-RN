import React from 'react';
import {
  AppRegistry,
  View,
} from 'react-native';

import PropertyFinder from './components/PropertyFinder';

const App = () => <PropertyFinder />;

export default App;
//AppRegistry.registerComponent('wenderlich', () => App);
